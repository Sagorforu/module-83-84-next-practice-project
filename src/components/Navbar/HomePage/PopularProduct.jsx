import wait from "@/utils/wait";

const PopularProduct =async () => {
     await wait()
     return (
          <div>
               Popular product
          </div>
     );
};

export default PopularProduct;